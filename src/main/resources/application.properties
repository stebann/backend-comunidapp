# Nombre de la aplicación
spring.application.name=comunidapp-backend

# --- CONFIGURACIÓN DEL SERVIDOR ---
server.port=${PORT:8080}

# --- CONFIGURACIÓN PARA RAILWAY ---
# En desarrollo local y producción, apunta a la base de datos de Railway
# Si Railway inyecta variables de entorno, las usará. Si no, usa los valores por defecto de Railway.
spring.datasource.url=jdbc:mysql://${DB_HOST:centerbeam.proxy.rlwy.net}:${DB_PORT:35099}/${DB_NAME:comunidapp}?createDatabaseIfNotExist=true&useSSL=true&requireSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:jrDUdultdccWJtndJmOdATUQOEYaTHKN}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Configuración de la zona horaria
spring.jackson.time-zone=America/Bogota

# Swagger
springdoc.swagger-ui.path=/
springdoc.swagger-ui.url=/v3/api-docs

# --- CONFIGURACIÓN DE CLOUDINARY ---
# Configuración para almacenamiento de imágenes en Cloudinary
# Obtén estos valores desde tu panel de Cloudinary: https://console.cloudinary.com/
#
# OPCIÓN 1: Usar CLOUDINARY_URL (recomendado por Cloudinary)
# Formato: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
# Si defines la variable de entorno CLOUDINARY_URL, Cloudinary la usará automáticamente
#
# OPCIÓN 2: Usar propiedades individuales (alternativa)
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dp92gh9t6}
cloudinary.api-key=${CLOUDINARY_API_KEY:439553298123895}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:OiRkx67fkfodaqEuo6qYScDN5Sg}
cloudinary.folder=${CLOUDINARY_FOLDER:comunidapp}
